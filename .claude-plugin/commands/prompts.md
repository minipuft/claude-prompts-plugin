---
name: prompts
description: List available prompt templates from claude-prompts-mcp
---

List the available prompt templates from the claude-prompts-mcp server.

Use the `mcp__claude-prompts-mcp__resource_manager` tool with:
- `resource_type`: "prompt"
- `action`: "list"

Display the results in a concise table format showing prompt ID, description, and category.
