---
name: gates
description: List available quality gates for output validation
---

List the available quality gates from the claude-prompts-mcp server.

Use the `mcp__claude-prompts-mcp__system_control` tool with:
- `action`: "gates"
- `operation`: "list"

Display the results showing gate ID, severity, and validation criteria.
